const legacySeed=[
['20000-1102','ภาษาไทยเพื่ออาชีพ','อมรรัตน์ ฉายศรี','ศูนย์หนังสือเมืองไทย',90],['20000-1203','การฟังและพูดภาษาอังกฤษ','พิมพ์ใจ สายวิภู','ศูนย์หนังสือเมืองไทย',90],['20000-1403','คณิตศาสตร์ไฟฟ้าและอิเล็กทรอนิกส์','วราภรณ์ วงศ์ไตรรัตน์','ศูนย์หนังสือเมืองไทย',109],['20000-1502','ประวัติศาสตร์ชาติไทย','พิสิฐพงศ์ สีดาว','ศูนย์หนังสือเมืองไทย',99],['20001-1001','สุขภาพความปลอดภัยและสิ่งแวดล้อม','ดร.ทัชพงษ์ จันทร์ลี','ศูนย์หนังสือเมืองไทย',119],['20100-1003','งานฝึกฝีมือ','อ.ชลอ การทวี','สำนักพิมพ์เอมพันธ์',105],['20100-1006','งานเครื่องมือกลเบื้องต้น','อ.ชลอ การทวี','สำนักพิมพ์เอมพันธ์',83],['20104-2003','วงจรไฟฟ้ากระแสสลับ','สมศักดิ์ แก้วพันธ์','สำนักพิมพ์เอมพันธ์',119],['20104-2012','การติดตั้งไฟฟ้านอกอาคาร','อ.คม แรงสูงเนิน','ศูนย์หนังสือเมืองไทย',110],['20104-2013','หม้อแปลงไฟฟ้า','อ.สว่าง จันทร์ขาว','สำนักพิมพ์เอมพันธ์',99],['20104-2023','การส่องสว่าง','อ.ณัฐธีร์ ธีธนาวิโรจน์','สำนักพิมพ์เอมพันธ์',67],['20100-1007','งานนิวแมติกส์และไฮดรอลิกส์เบื้องต้น','อ.กิตติพันธ์ ไทยสงค์','สำนักพิมพ์เอมพันธ์',125],['20104-2010','การประมาณการติดตั้งไฟฟ้า','อ.ธำรงศักดิ์ หมินก้าหรีม','ศูนย์หนังสือเมืองไทย',120],['20104-2014','เครื่องกำเนิดไฟฟ้ากระแสสลับ','อ.สุธน แก่นต้น','ศูนย์หนังสือเมืองไทย',110],['20104-2015','เครื่องปรับอากาศ','เฉลิมรัตน์ ชมภูประเภท','ศูนย์หนังสือเมืองไทย',120],['20104-2027','คณิตศาสตร์ไฟฟ้า','ธำรงศักดิ์ หมื่นก้าหรีม','สำนักพิมพ์เอมพันธ์',109],['20104-2017','กฎและมาตรฐานทางไฟฟ้า','อ.สุขสันต์ หวังสถิตวงษ์','ศูนย์ส่งเสริมวิชาการ',114],['20104-2020','ดิจิทัลเบื้องต้น','สุชิน ชินสีห์','ศูนย์หนังสือเมืองไทย',100]
];
window.BOOK_LEGACY=legacySeed.map(x=>({code:x[0],title:x[1],author:x[2],publisher:x[3],price:x[4],status:"ใช้",source:"2568"}));
/* v51: isolated free-book workspace. Never rewrites scheduling records. */
(()=>{'use strict';
const $=id=>document.getElementById(id), esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])), K='college_free_books_v51', W='http://schemas.openxmlformats.org/wordprocessingml/2006/main';
const read=(k,f)=>{const raw=localStorage.getItem(k);if(!raw)return f;try{return JSON.parse(raw)}catch(e){throw Error('ข้อมูลที่บันทึกไว้เสียหาย: '+k+' กรุณาสำรองก่อนแก้ไข')}};
let data;try{data=read(K,{version:51,plans:{},history:[]})}catch(e){alert(e.message);return}
const saveBooks=()=>{try{localStorage.setItem(K,JSON.stringify(data))}catch(e){alert('บันทึกไม่สำเร็จ พื้นที่อาจเต็ม กรุณาส่งออกข้อมูลสำรอง');throw e}};
const uid=()=>crypto.randomUUID?.()||('b'+Date.now()+Math.random().toString(36).slice(2));
const plans=()=>state.plans||[], active=()=>plans().find(p=>p.id===$('bkPlan').value), subjects=(p=active())=>(state.subjects||[]).filter(s=>s.planId===p?.id);
const ctx=(p=active())=>{if(!p)return null;return data.plans[p.id]||(data.plans[p.id]={books:{},extra:[],meta:{}})};
const courses=(p=active())=>{const a=[...subjects(p),...(ctx(p)?.extra||[])];return a.filter((s,i)=>a.findIndex(x=>norm(x.code)===norm(s.code))===i)};
function norm(s){return String(s??'').replace(/[๐-๙]/g,c=>'๐๑๒๓๔๕๖๗๘๙'.indexOf(c)).replace(/[–—−]/g,'-').trim()}
const picks=(s,p=active())=>ctx(p)?.books[norm(s.code)]||[];
const chosen=(s,p=active())=>picks(s,p)[0];
const valid=b=>b&&b.title?.trim()&&b.author?.trim()&&b.publisher?.trim()&&Number.isFinite(+b.price)&&+b.price>0&&b.status==='ใช้'&&b.confirmed;
const money=n=>Number(n).toFixed(2);
const docMoney=n=>Number.isInteger(+n)?String(+n):money(n);
function status(s,bad=false){$('bkExportStatus').textContent=s;$('bkExportStatus').className='notice '+(bad?'warn':'good')}
function dl(blob,name){const a=document.createElement('a');const u=URL.createObjectURL(blob);a.href=u;a.download=name;document.body.append(a);a.click();setTimeout(()=>{URL.revokeObjectURL(u);a.remove()},30000)}
const safe=s=>String(s).replace(/[\\/:*?"<>|]/g,'-');
function audit(action,code,before,after,reason=''){data.history.push({at:new Date().toISOString(),plan:active()?.id,action,code,before,after,reason})}
const clone=o=>JSON.parse(JSON.stringify(o));
// Existing v1 book data is copied lazily per plan; original key remains intact.
function migratePlan(){const c=ctx();if(!c||c.migrated)return;const legacy=read('college_free_books_v1',window.BOOK_LEGACY||[]);for(const s of subjects()){const found=legacy.filter(b=>norm(b.code)===norm(s.code)&&b.status!=='เลิกใช้').slice(0,3);if(found.length)c.books[norm(s.code)]=found.map(b=>({...b,id:uid(),confirmed:false}))}c.migrated=true;saveBooks()}
$('bkFile').accept='.xlsx,.docx,.csv,.json';
$('bkSem').insertAdjacentHTML('beforeend','<option value="summer">ภาคฤดูร้อน</option>');
$('bkSync').textContent='ตรวจรายวิชา / รีเฟรช';
$('bkExportSmo').textContent='ส่งออก สมอ.1–4 (ZIP)';$('bkExportAll').textContent='สร้างเอกสารทั้งชุด (ZIP)';
$('bkPdfHelp').textContent='วิธีตรวจหน้าและบันทึก PDF';
$('bkRows').closest('.card').insertAdjacentHTML('beforebegin','<div class="card"><h2>ตรวจความครบก่อนส่ง</h2><div id="bkDashboard"></div><div class="actions"><button class="btn light" id="bkBackup">สำรองข้อมูลหนังสือ</button><button class="btn light" id="bkHistory">ประวัติการเปลี่ยนหนังสือ</button></div><p class="muted">ข้อมูลหนังสือบันทึกในเบราว์เซอร์เครื่องนี้ แยกจากข้อมูลตารางสอน ควรสำรองก่อนย้ายเครื่อง</p></div>');
$('bkStudents').parentElement.insertAdjacentHTML('afterend','<label>วงเงินหนังสือต่อคน (บาท)<input id="bkBudget" type="number" min="0" step="0.01" placeholder="ไม่กำหนด"></label>');
const modal=document.createElement('dialog');modal.id='bkDialog';modal.style.cssText='width:min(1000px,94vw);max-height:90vh;overflow:auto;border:1px solid #cbd5e1;border-radius:12px;padding:24px';document.body.append(modal);
const show=html=>{modal.innerHTML=html;modal.showModal()};
function init(prefer){const old=prefer||$('bkPlan').value;const ps=plans().filter(p=>String(p.academicYear)===$('bkYear').value.trim()&&String(p.semester)===$('bkSem').value);$('bkPlan').innerHTML=ps.length?ps.map(p=>`<option value="${esc(p.id)}">${esc(p.level)} ปี ${esc(p.year)} • ${esc(p.term)}</option>`).join(''):'<option value="">ไม่มีแผนในภาคเรียนนี้</option>';if(ps.some(p=>p.id===old))$('bkPlan').value=old;loadMeta();render()}
function loadMeta(){if(!active())return;migratePlan();const m=ctx().meta;for(const id of ['bkDate','bkHead','bkStudents','bkBudget'])$(id).value=m[id]??({bkDate:'',bkHead:state.signers?.dept||'นายสุปกาณฑ์ ไพรนอก',bkStudents:'',bkBudget:''}[id])}
function render(){const p=active(), ss=courses();$('bkRows').innerHTML=ss.map((s,i)=>{const b=chosen(s);return `<tr><td>${esc(s.code)}</td><td><b>${esc(s.name)}</b><div>${esc(b?.title||'ยังไม่เลือกหนังสือ')}</div></td><td>${esc(b?.author||'—')}</td><td>${esc(b?.publisher||'—')}</td><td>${b?docMoney(b.price):'—'}</td><td class="${valid(b)?'bk-ok':'bk-wait'}">${!b?'ขาดหนังสือ':!b.confirmed?'รอตรวจยืนยัน':esc(b.status)}</td><td><button class="btn light" data-edit="${i}">ตัวเลือก 1–3 / แก้ไข</button></td></tr>`}).join('');$('bkStatus').textContent=p?`${p.level} ปี ${p.year} • ${p.term} • ${ss.length} วิชา`:'ไม่มีแผนสำหรับปี / ภาคเรียนที่เลือก กรุณาเพิ่มหรือนำเข้าแผนในเมนูแผนการเรียน';$('bkRows').querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>edit(ss[+b.dataset.edit]));
const sum=ss.reduce((n,s)=>n+(Number(chosen(s)?.price)||0),0);let html=plans().filter(q=>String(q.academicYear)===$('bkYear').value&&String(q.semester)===$('bkSem').value).map(q=>{const all=courses(q),ok=all.filter(s=>valid(chosen(s,q))).length;return `<button class="btn light" data-plan="${esc(q.id)}">${esc(q.level)}${esc(q.year)}: ${ok}/${all.length} วิชาครบ${ok===all.length&&ok?' ✓':''}</button>`}).join(' ');const budget=+$('bkBudget').value;html+=`<p>แผนที่เลือก: รวมต่อชุด <b>${money(sum)}</b> บาท • ${$('bkStudents').value?`ยอดตามจำนวนผู้เรียน ${money(sum*(+$('bkStudents').value||0))} บาท`:'ยังไม่ระบุจำนวนผู้เรียน'}${budget&&sum>budget?' • ⚠ เกินวงเงิน '+money(sum-budget)+' บาท':''}</p><p>สมอ.1–4 ใช้รายการที่เลือกชุดเดียวกัน • ระบบตรวจจำนวนแถวก่อนส่งออก • ต้องเปิด Word ตรวจหน้าอีกครั้ง</p>`;$('bkDashboard').innerHTML=html;$('bkDashboard').querySelectorAll('[data-plan]').forEach(b=>b.onclick=()=>{init(b.dataset.plan)});status('พร้อมตรวจข้อมูลและสร้างเอกสาร • การตรวจโครงสร้างไม่ใช่การรับรองการจัดหน้าของ Word')}
function edit(s){const existing=picks(s), ss=clone(existing);show(`<h2>${esc(s.code)} ${esc(s.name)}</h2><p>อันดับ 1 คือเล่มที่จะจัดซื้อ กดใช้เล่มนี้เพื่อสลับอันดับ พร้อมบันทึกเหตุผล</p><form id="bkEdit"><div id="bkAlternatives"></div><label>เหตุผลที่แก้ไข / เปลี่ยนเล่ม<input name="reason" required></label><div class="actions"><button class="btn primary">บันทึกและยืนยันข้อมูล</button><button class="btn light" type="button" id="bkCancel">ยกเลิก</button></div></form>`);
function fields(){ $('bkAlternatives').innerHTML=Array.from({length:3},(_,i)=>{const b=ss[i]||{};return `<fieldset><legend>อันดับ ${i+1}${i===0?' (เลือกจัดซื้อ)':''}</legend><div class="grid3">${[['title','ชื่อหนังสือ'],['author','ผู้แต่ง'],['publisher','สำนักพิมพ์'],['price','ราคา'],['approval','เลขอนุญาต'],['approvalYear','ปีที่อนุญาต'],['paper','กระดาษ (ปอนด์/ถนอมสายตา/อาร์ต)'],['colors','จำนวนสี (1/2/4)'],['size','ขนาด (8หน้ายก/A4/อื่น ๆ)'],['reason','เหตุผลที่เลือก']].map(([k,t])=>`<label>${t}<input data-i="${i}" data-k="${k}" value="${esc(b[k]??'')}" ${k==='price'?'type="number" min="0.01" step="0.01"':''}></label>`).join('')}<label>สถานะ<select data-i="${i}" data-k="status">${['ใช้','หมด','สำรอง','เลิกใช้'].map(v=>`<option ${v===(b.status||'ใช้')?'selected':''}>${v}</option>`).join('')}</select></label></div>${i?`<button class="btn light" type="button" data-use="${i}">ใช้เล่มนี้แทนอันดับ 1</button>`:''}</fieldset>`}).join('');$('bkAlternatives').querySelectorAll('[data-use]').forEach(b=>b.onclick=()=>{collect();const i=+b.dataset.use;if(!ss[i]?.title)return alert('กรอกข้อมูลหนังสือก่อน');if(ss[i].status==='หมด'||ss[i].status==='เลิกใช้')return alert('เล่มนี้ไม่พร้อมจัดซื้อ');[ss[0],ss[i]]=[ss[i],ss[0]];ss[0].status='ใช้';fields()})}
function collect(){modal.querySelectorAll('[data-k]').forEach(e=>{const i=+e.dataset.i;ss[i]=ss[i]||{id:uid(),code:norm(s.code)};ss[i][e.dataset.k]=e.dataset.k==='price'?(e.value===''?null:Number(e.value)):e.value.trim()})}fields();$('bkCancel').onclick=()=>modal.close();$('bkEdit').onsubmit=e=>{e.preventDefault();collect();const books=ss.filter(b=>b.title);if(!books.length||ss[0]!==books[0])return alert('กรอกหนังสืออันดับ 1');if(books.some(b=>!b.author||!b.publisher||!Number.isFinite(b.price)||b.price<=0))return alert('กรอกผู้แต่ง สำนักพิมพ์ และราคาให้ครบ');books.forEach(b=>b.confirmed=true);const reason=new FormData(e.target).get('reason');audit('แก้ไข/เลือกหนังสือ',s.code,clone(existing),clone(books),reason);ctx().books[norm(s.code)]=books;saveBooks();modal.close();render()}}
$('bkAdd').onclick=()=>{if(!active())return alert('เลือกแผนก่อน');const code=norm(prompt('รหัสวิชา')||'');if(!code)return;if(!/^\d{5}-\d{4}$/.test(code))return alert('รหัสวิชาต้องเป็น 00000-0000');let s=courses().find(s=>norm(s.code)===code);if(!s){const name=prompt('ชื่อวิชา');if(!name?.trim())return;s={code,name:name.trim()};ctx().extra.push(s);saveBooks();render()}edit(s)};
$('bkSync').onclick=()=>init();$('bkSem').onchange=()=>init();$('bkYear').onchange=()=>init();$('bkPlan').onchange=()=>{loadMeta();render()};
for(const id of ['bkDate','bkHead','bkStudents','bkBudget'])$(id).onchange=()=>{if(ctx()){ctx().meta[id]=$(id).value;saveBooks();render()}};
$('bkBackup').onclick=()=>dl(new Blob([JSON.stringify(data,null,2)],{type:'application/json'}),'ข้อมูลหนังสือ-v51.json');
$('bkHistory').onclick=()=>{show('<h2>ประวัติการเปลี่ยนหนังสือ</h2>'+data.history.filter(h=>h.plan===active()?.id).slice().reverse().map(h=>`<p>${esc(new Date(h.at).toLocaleString('th-TH'))} • ${esc(h.code)} • ${esc(h.action)}<br>${esc(h.before?.[0]?.title||'—')} → ${esc(h.after?.[0]?.title||'—')}<br>เหตุผล: ${esc(h.reason)}</p>`).join('')+'<button class="btn" id="bkCloseHistory">ปิด</button>');$('bkCloseHistory').onclick=()=>modal.close()};
const parse=xml=>{const d=new DOMParser().parseFromString(xml,'application/xml');if(d.querySelector('parsererror'))throw Error('โครงสร้าง XML ไม่ถูกต้อง');return d};const els=(n,k)=>[...n.getElementsByTagNameNS('*',k)];const txt=n=>els(n,'t').map(n=>n.textContent).join('');
async function matrices(file){if(file.size>20*1024*1024)throw Error('ไฟล์ใหญ่เกิน 20 MB');if(/\.csv$/i.test(file.name)){const t=await file.text(),rows=[];let row=[],v='',q=false;for(let i=0;i<t.length;i++){const c=t[i];if(c==='"'){if(q&&t[i+1]==='"'){v+='"';i++}else q=!q}else if(c===','&&!q){row.push(v);v=''}else if(c==='\n'&&!q){row.push(v.replace(/\r$/,''));rows.push(row);row=[];v=''}else v+=c}row.push(v);rows.push(row);return [rows]}
const z=await JSZip.loadAsync(await file.arrayBuffer());if(/\.docx$/i.test(file.name)){const d=parse(await z.file('word/document.xml').async('string'));return els(d,'tbl').map(t=>els(t,'tr').map(r=>[...r.children].filter(c=>c.localName==='tc').map(txt)))}if(!/\.xlsx$/i.test(file.name))throw Error('รองรับ .xlsx .docx .csv กรุณาแปลง .xls เป็น .xlsx');const shared=z.file('xl/sharedStrings.xml')?els(parse(await z.file('xl/sharedStrings.xml').async('string')),'si').map(txt):[];const out=[];for(const name of Object.keys(z.files).filter(n=>/^xl\/worksheets\/sheet\d+\.xml$/.test(n))){const d=parse(await z.file(name).async('string'));out.push(els(d,'row').map(r=>{const a=[];for(const c of els(r,'c')){const col=(c.getAttribute('r')||'A').match(/^[A-Z]+/)[0];let idx=0;for(const ch of col)idx=idx*26+ch.charCodeAt(0)-64;const v=els(c,'v')[0]?.textContent||'';a[idx-1]=c.getAttribute('t')==='s'?shared[+v]:c.getAttribute('t')==='inlineStr'?txt(c):v}return a}))}return out}
function importedRows(ms){const found=[],aliases={code:['รหัสวิชา','รหัสรายวิชา','รหัส','coursecode'],title:['ชื่อหนังสือ','ชื่อวิชา','หนังสือ','วิชา','title'],author:['ชื่อผู้แต่ง','ผู้แต่ง','author'],publisher:['สำนักพิมพ์','บริษัท','publisher'],price:['ราคาต่อเล่ม','ราคาหนังสือ','ราคา','price']};for(const rows of ms){let map=null;for(const row of rows){const hs=row.map(v=>String(v??'').toLowerCase().replace(/\s/g,''));const m={};for(const [k,names]of Object.entries(aliases))m[k]=hs.findIndex(h=>names.includes(h));if(Object.values(m).every(i=>i>=0)){map=m;continue}if(!map)continue;const b=Object.fromEntries(Object.entries(map).map(([k,i])=>[k,String(row[i]??'').trim()]));b.code=norm(b.code);if(!/^\d{5}-\d{4}$/.test(b.code))continue;b.price=Number(norm(b.price).replace(/[,฿\s]/g,''));found.push(b)}}return found}
async function importFile(f){if(!active())return alert('เลือกแผนก่อน');try{if(/\.json$/i.test(f.name)){const incoming=JSON.parse(await f.text());if(incoming.version!==51||!incoming.plans||!Array.isArray(incoming.history))throw Error('ไม่ใช่ไฟล์สำรอง v51');for(const c of Object.values(incoming.plans)){if(!c||!c.books||!Array.isArray(c.extra)||!c.meta)throw Error('โครงสร้างไฟล์สำรองไม่ถูกต้อง');for(const a of Object.values(c.books))if(!Array.isArray(a)||a.length>3||a.some(b=>!b||typeof b.title!=='string'||typeof b.code!=='string'||!Number.isFinite(+b.price)))throw Error('ข้อมูลหนังสือในไฟล์สำรองไม่ถูกต้อง')}if(!confirm('คืนข้อมูลหนังสือจากไฟล์นี้แทนข้อมูลหนังสือปัจจุบัน? ตารางสอนไม่เปลี่ยน'))return;dl(new Blob([JSON.stringify(data)],{type:'application/json'}),'สำรองก่อนคืนข้อมูล.json');data=incoming;saveBooks();init();return}const rows=importedRows(await matrices(f));if(!rows.length)throw Error('ไม่พบตารางที่มีหัวคอลัมน์ รหัสวิชา / ชื่อหนังสือ / ผู้แต่ง / สำนักพิมพ์ / ราคา');const pid=active().id;const ss=courses();show(`<h2>ตรวจข้อมูลก่อนนำเข้า ${esc(f.name)}</h2><p>รายการรหัสตรงจะเพิ่มเป็นตัวเลือกสำรอง ไม่เขียนทับเล่มที่เลือก รายการไม่ตรงแผนต้องเลือกวิชาปลายทางเอง</p><div style="overflow:auto"><table><thead><tr><th>เลือก</th><th>รหัส / หนังสือ / ผู้แต่ง / สำนักพิมพ์ / ราคา</th><th>จับคู่รายวิชา</th></tr></thead><tbody>${rows.map((b,i)=>`<tr><td><input type="checkbox" data-row="${i}" ${ss.some(s=>norm(s.code)===b.code)?'checked':''}></td><td>${esc(b.code)} • ${esc(b.title)}<br>${esc(b.author)} • ${esc(b.publisher)} • ${docMoney(b.price)}</td><td><select data-match="${i}"><option value="">ไม่จับคู่</option>${ss.map(s=>`<option value="${esc(s.code)}" ${norm(s.code)===b.code?'selected':''}>${esc(s.code)} ${esc(s.name)}</option>`).join('')}<option value="+">เพิ่มวิชาสามัญในงานหนังสือ</option></select></td></tr>`).join('')}</tbody></table></div><p id="bkImportError"></p><button class="btn primary" id="bkConfirmImport">ยืนยันนำเข้ารายการที่เลือก</button> <button class="btn" id="bkCancelImport">ยกเลิก</button>`);$('bkCancelImport').onclick=()=>modal.close();$('bkConfirmImport').onclick=()=>{try{if(active()?.id!==pid)throw Error('แผนเปลี่ยน กรุณานำเข้าใหม่');const next=clone(ctx()),logs=[];let count=0;for(const e of modal.querySelectorAll('[data-row]:checked')){const i=+e.dataset.row,b={...rows[i]},target=modal.querySelector(`[data-match="${i}"]`).value;if(!target)throw Error('เลือกวิชาปลายทางให้ครบ');if(!b.title||!b.author||!b.publisher||!Number.isFinite(b.price)||b.price<=0)throw Error('ข้อมูลไม่ครบ/ราคาไม่ถูกต้อง: '+b.code);if(target==='+'){if(!ss.some(s=>norm(s.code)===b.code)&&!next.extra.some(s=>s.code===b.code))next.extra.push({code:b.code,name:b.title})}else b.code=norm(target);const a=next.books[b.code]||(next.books[b.code]=[]);if(a.some(x=>x.title===b.title&&x.author===b.author&&x.publisher===b.publisher&&+x.price===b.price))continue;if(a.length>=3)throw Error(b.code+' มีตัวเลือกครบ 3 แล้ว กรุณาแก้ไขตัวเลือกก่อน');const before=clone(a);a.push({...b,id:uid(),status:a.length?'สำรอง':'ใช้',confirmed:true,source:f.name});logs.push({code:b.code,before,after:clone(a)});count++}if(!count)throw Error('ไม่มีรายการใหม่ที่เลือก');data.plans[pid]=next;for(const l of logs)audit('นำเข้า',l.code,l.before,l.after,f.name);saveBooks();modal.close();render();status('นำเข้าสำเร็จ '+count+' รายการ')}catch(e){$('bkImportError').textContent=e.message}}}catch(e){status('นำเข้าไม่สำเร็จ: '+e.message,true)}}
$('bkImport').onclick=()=>$('bkFile').click();$('bkFile').onchange=e=>{const f=e.target.files[0];if(f)importFile(f);e.target.value=''};const drop=$('bookPage');drop.addEventListener('dragover',e=>{e.preventDefault()});drop.addEventListener('drop',e=>{e.preventDefault();if(e.dataTransfer.files[0])importFile(e.dataTransfer.files[0])});
function replaceText(node,re,value){const ts=els(node,'t'),full=ts.map(t=>t.textContent).join(''),matches=[...full.matchAll(new RegExp(re.source,re.flags.includes('g')?re.flags:re.flags+'g'))];for(const m of matches.reverse()){let start=m.index,end=start+m[0].length,pos=0;const spans=ts.map(t=>{const o={t,a:pos,b:pos+t.textContent.length};pos=o.b;return o});let inserted=false;for(const s of spans){if(s.b<=start||s.a>=end)continue;const a=Math.max(0,start-s.a),b=Math.min(s.t.textContent.length,end-s.a);s.t.textContent=s.t.textContent.slice(0,a)+(inserted?'':String(typeof value==='function'?value(m):value))+s.t.textContent.slice(b);s.t.setAttributeNS('http://www.w3.org/XML/1998/namespace','xml:space','preserve');inserted=true}}}
function setText(node,value){let ts=els(node,'t');if(!ts.length){let p=els(node,'p')[0];if(!p){p=node.ownerDocument.createElementNS(W,'w:p');node.append(p)}let r=els(p,'r')[0];if(!r){r=node.ownerDocument.createElementNS(W,'w:r');p.append(r)}const t=node.ownerDocument.createElementNS(W,'w:t');r.append(t);ts=[t]}ts[0].textContent=String(value??'');ts[0].setAttributeNS('http://www.w3.org/XML/1998/namespace','xml:space','preserve');ts.slice(1).forEach(t=>t.textContent='')}
const cells=r=>[...r.children].filter(n=>n.localName==='tc');
function shape(doc){const ser=new XMLSerializer();return JSON.stringify(['tblPr','tblGrid','trPr','tcPr','sectPr','pPr','rPr'].map(k=>els(doc,k).map(n=>ser.serializeToString(n))))}
function specs(b){return [b.paper==='ปอนด์'?'/':'',b.paper==='ถนอมสายตา'?'/':'',b.paper==='อาร์ต'?'/':'',String(b.colors)==='1'?'/':'',String(b.colors)==='2'?'/':'',String(b.colors)==='4'?'/':'',b.size==='8หน้ายก'?'/':'',b.size==='A4'?'/':'',b.size==='อื่น ๆ'?'/':'']}
const teacher=s=>(state.teachers||[]).find(t=>t.id===s.teacherId)?.name||'';
async function documentFor(kind,ss,publisher){const p=active(),c=+p.year;if(p.level!=='ปวช.'||![1,2,3].includes(c))throw Error('มีแบบฟอร์มต้นฉบับเฉพาะ ปวช.1–3');const encoded=window.BOOK_TEMPLATES[`${kind}-${c}`];const zip=await JSZip.loadAsync(encoded,{base64:true}),d=parse(await zip.file('word/document.xml').async('string')),before=shape(d),ps=els(d,'p'),table=els(d,'tbl')[0],rs=[...table.children].filter(n=>n.localName==='tr');const begin={memo:1,smo1:2,smo2:2,smo3:3,smo4:2}[kind],end=kind==='memo'?rs.length-1:rs.length;const capacity=kind==='smo1'?3:end-begin;
if(ss.length>capacity)throw Error(`${kind} มีพื้นที่ ${capacity} รายการ แต่มีข้อมูล ${ss.length} รายการ กรุณาจัดแบบฟอร์มเพิ่มเติมก่อน ไม่ได้ตัดรายการหรือย่อตัวอักษร`);
let teacherIndex=0;const selectedTeachers=[...new Set(ss.map(x=>teacher(x.s)).filter(Boolean))];
const sem=$('bkSem').value==='summer'?'ฤดูร้อน':$('bkSem').value,year=$('bkYear').value;
for(const para of ps){if(para.closest?.('w\\:tc'))continue; // only body paragraphs below are eligible
 if(els(table,'p').includes(para))continue;
 replaceText(para,/ภาคเรียนที่[.\s]*[12๑๒]/g,m=>m[0].replace(/[12๑๒]$/,sem));
 replaceText(para,/๒๙\s*กันยายน\s*2568/g,$('bkDate').value);
 replaceText(para,/2568/g,year);
 replaceText(para,/นายสุปกาณฑ์\s+ไพรนอก/g,$('bkHead').value);
 if(kind==='memo'){replaceText(para,/จำนวน\s*[0-9๐-๙]+\s*คน/g,'จำนวน '+$('bkStudents').value+' คน')}
 if(kind==='smo1'){
 const s=ss[0].s;
 replaceText(para,/รหัสวิชา\s+\S+\s+ชื่อวิชา\s+.*?(?=\s+ระดับชั้น)/g,`รหัสวิชา  ${s.code}  ชื่อวิชา  ${s.name}`);
 replaceText(para,/ครูผู้สอน\.+.*?\.+(?=\s*$)/g,`ครูผู้สอน..........${teacher(s)}..........`);
 if ((txt(para).match(/\(/g)||[]).length>=3) replaceText(para,/^(\s*)\([^)]*\)/,m=>m[1]+'('+teacher(s)+')');
 }
 if(kind==='smo4'&&(txt(para).match(/\(/g)||[]).length>=3){teacherIndex=(txt(para).match(/\(/g)||[]).length;replaceText(para,/\([^)]*\)/g,()=> '('+(selectedTeachers[--teacherIndex]||'...........................................')+')')}
 if(kind==='smo3'&&/^\s*สำนักพิมพ์/.test(txt(para)))setText(para,publisher);
}
for(let i=begin;i<end;i++){const cs=cells(rs[i]),x=ss[i-begin];cs.forEach(c=>setText(c,''));if(!x)continue;const b=x.b,s=x.s,n=i-begin+1;let values;
if(kind==='memo')values=[n,s.code,b.title,b.author,b.publisher,docMoney(b.price)];
if(kind==='smo1')values=[n,b.approval||'',b.approvalYear||'',b.author,b.publisher,...specs(b),docMoney(b.price),b.reason||''];
if(kind==='smo2')values=[n,b.title,b.author,b.publisher,docMoney(b.price),teacher(s),'',''];
if(kind==='smo3')values=[n,b.title,b.author,...specs(b),docMoney(b.price),'',''];
if(kind==='smo4')values=[n,b.title,b.author,b.publisher,docMoney(b.price),'','','','','',''];
values.forEach((v,j)=>{if(cs[j])setText(cs[j],v)})}
if(kind==='memo'){const cs=cells(rs[rs.length-1]);setText(cs[0],$('bkBudget').value?`รวม (วงเงิน ${money($('bkBudget').value)} บาท)`:'รวม');setText(cs.at(-1),money(ss.reduce((n,x)=>n+(+x.b.price),0)))}
if(shape(d)!==before)throw Error('โครงสร้างรูปแบบเปลี่ยน ระบบหยุดส่งออกเพื่อป้องกันแบบฟอร์มผิดเพี้ยน');
zip.file('word/document.xml',new XMLSerializer().serializeToString(d));return {bytes:await zip.generateAsync({type:'uint8array'}),kind,rows:ss.length,capacity,structure:'ผ่าน: ระยะขอบ ตาราง ขนาดคอลัมน์ ความสูงแถว และรูปแบบตัวอักษรคงเดิม',pageCheck:'ยังไม่ได้ตรวจด้วย Microsoft Word; ต้องตรวจหน้าและลายเซ็นก่อนใช้จริง'};
}
function preflight(){if(!active())throw Error('เลือกแผนการเรียนก่อน');const ss=courses();if(!ss.length)throw Error('แผนนี้ไม่มีรายวิชา');const missing=ss.filter(s=>!valid(chosen(s)));if(missing.length)throw Error('ข้อมูลยังไม่ครบ / รอยืนยัน / หนังสือหมด: '+missing.map(s=>s.code).join(', '));if(!$('bkDate').value.trim()||!$('bkHead').value.trim())throw Error('ระบุวันที่เอกสารและหัวหน้าแผนก');if(!$('bkStudents').value||+$('bkStudents').value<=0||!Number.isInteger(+$('bkStudents').value))throw Error('ระบุจำนวนผู้เรียนเป็นจำนวนเต็มมากกว่า 0');const sum=ss.reduce((n,s)=>n+(+chosen(s).price),0);if(+$('bkBudget').value>0&&sum>+$('bkBudget').value)throw Error('ราคาต่อชุดเกินวงเงิน กรุณาตรวจสอบรายการ / วงเงินก่อนส่งออก');return ss}
let exporting=false;
async function exportSet(mode){if(exporting)return;exporting=true;const buttons=['bkExportMemo','bkExportSmo','bkExportAll'];buttons.forEach(id=>$(id).disabled=true);try{const ss=preflight(),rows=ss.map(s=>({s,b:chosen(s)})),jobs=[];status('กำลังตรวจและสร้างเอกสารจากข้อมูลชุดเดียวกัน…');if(mode!=='smo')jobs.push(['บันทึกข้อความ',await documentFor('memo',rows)]);if(mode!=='memo'){for(const s of ss)jobs.push([`สมอ1-${safe(s.code)}`,await documentFor('smo1',picks(s).map(b=>({s,b})))]);jobs.push(['สมอ2',await documentFor('smo2',rows)]);for(const [pubIndex,pub] of [...new Set(rows.map(x=>x.b.publisher))].entries())jobs.push([`สมอ3-${pubIndex+1}-${safe(pub)}`,await documentFor('smo3',rows.filter(x=>x.b.publisher===pub),pub)]);jobs.push(['สมอ4',await documentFor('smo4',rows)])}
if(mode!=='memo'&&!confirm('ก่อนดาวน์โหลด: ระบบตรวจโครงสร้างและจำนวนแถวแล้ว แต่ยังตรวจหน้าแบบ Microsoft Word อัตโนมัติไม่ได้ โดยเฉพาะลายเซ็น สมอ.4 อาจไหลไปหน้าใหม่เมื่อฟอนต์หรือข้อความต่างจากต้นฉบับ กรุณาเปิดตรวจทุกหน้าก่อนใช้จริง ต้องการดาวน์โหลดเพื่อตรวจหรือไม่?'))return;
const long=rows.filter(x=>x.b.title.length>45||x.b.author.length>35||x.b.publisher.length>35);if(long.length&&!confirm('ข้อความยาวอาจทำให้แถวสูงหรือหน้าเพิ่มใน Word: '+long.map(x=>x.s.code).join(', ')+'\nระบบคงขนาดตัวอักษรเดิม กรุณาตรวจหน้าหลังเปิดไฟล์ ต้องการดาวน์โหลดเพื่อตรวจหรือไม่?'))return;
const prefix=`ปวช${active().year}_${$('bkSem').value}-${$('bkYear').value}`;
if(mode==='memo'){dl(new Blob([jobs[0][1].bytes],{type:'application/vnd.openxmlformats-officedocument.wordprocessingml.document'}),`บันทึกข้อความ_${prefix}.docx`)}else{const zip=new JSZip();for(const [name,r] of jobs)zip.file(prefix+'/'+name+'.docx',r.bytes);zip.file(prefix+'/ผลตรวจเอกสาร.json',JSON.stringify({created:new Date().toISOString(),plan:clone(active()),books:rows,checks:jobs.map(([name,{bytes,...r}])=>({name,...r}))},null,2));zip.file(prefix+'/อ่านก่อนใช้งาน.txt','เอกสารใช้แบบฟอร์มต้นฉบับ แยก สมอ.1 รายวิชา และ สมอ.3 รายสำนักพิมพ์\nระบบตรวจจำนวนแถวและโครงสร้าง แต่ไม่ได้รันตัวจัดหน้า Microsoft Word\nกรุณาเปิด Word ตรวจทุกหน้า รวมตำแหน่งลายเซ็นและข้อความยาว แล้ว Export PDF จาก Word\nช่องการอนุญาต/คุณสมบัติที่ไม่ได้กรอกจะว่าง และไม่เติมผลการพิจารณาหรือผลตรวจรับแทนคณะกรรมการ');dl(await zip.generateAsync({type:'blob'}),`หนังสือเรียนฟรี-v51_${prefix}.zip`)}status('สร้างเอกสารสำเร็จ '+jobs.length+' ฉบับ • ผ่านการตรวจโครงสร้างและจำนวนแถว • เปิด Word ตรวจหน้าก่อนใช้งาน');return jobs}catch(e){status('ส่งออกไม่สำเร็จ: '+e.message,true)}finally{exporting=false;buttons.forEach(id=>$(id).disabled=false)}}
$('bkExportMemo').onclick=()=>exportSet('memo');$('bkExportSmo').onclick=()=>exportSet('smo');$('bkExportAll').onclick=()=>exportSet('all');$('bkPdfHelp').onclick=()=>alert('ระบบคงรูปแบบ OOXML และตรวจแถวก่อนส่งออก แต่เว็บนี้ไม่มีตัวจัดหน้าของ Word จึงไม่สามารถตรวจภาพหน้าอัตโนมัติหรือรับรอง PDF ตรงต้นฉบับ 100%\nเปิดไฟล์ด้วย Microsoft Word ใช้ฟอนต์เดียวกับต้นฉบับ ตรวจตาราง ข้อความ ลายเซ็นและจำนวนหน้าก่อน Export / Save as PDF');
// Fix the locked term selector by switching existing plans, never renaming old plans.
const style=document.createElement('style');style.textContent='#bookPage{font-size:16px}#bookPage table{table-layout:auto!important;font-size:15px!important}#bookPage th,#bookPage td{font-size:15px!important;line-height:1.5!important;width:auto!important;white-space:normal!important}#bookPage .muted,#bookPage label{font-size:14px}#bookPage input,#bookPage select{font-size:15px}#planPage #semester{pointer-events:auto;background:white}#bkDialog input,#bkDialog select{max-width:100%;box-sizing:border-box}#bkDialog fieldset{margin:12px 0;border:1px solid #ddd;border-radius:8px}';document.head.append(style);
$('semester').onchange=()=>{const p=plan(),sem=$('semester').value,target=plans().find(x=>x.level===p.level&&String(x.year)===String(p.year)&&String(x.academicYear)===String(p.academicYear)&&String(x.semester)===sem);if(!target){$('semester').value=p.semester;alert('ยังไม่มีแผนภาคเรียนนี้สำหรับชั้นปีที่เลือก กรุณากดเพิ่มแผนหรือนำเข้าแผนการเรียน ระบบไม่เปลี่ยนชื่อแผนเดิมเพื่อรักษารายวิชาและตารางเดิม');return}state.activePlanId=target.id;renderAll();refreshSelectors();save()};
document.querySelectorAll('[data-page="bookPage"],button[onclick*="bookPage"]').forEach(b=>b.addEventListener('click',()=>setTimeout(()=>init(),0)));
const p=plan();if(p){$('bkYear').value=p.academicYear;$('bkSem').value=p.semester}init(p?.id);
window.BookV51={init,render,preflight,documentFor,exportSet,importedRows,matrices,getData:()=>clone(data)};
})();
